namespace Const
{
    public static class {{ name }}
    {
        {% for const in consts -%}
        public static readonly {{ const.type }} {{ const.name }} = {{ const.value }};
        {%- if not loop.last %}
        {% endif %}
        {%- endfor %}
    }
}
