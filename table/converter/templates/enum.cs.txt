// DO NOT MODIFY DIRECTLY

using System;

namespace Shared.Type
{
    public enum {{ name }}
    {
        {% for scope, item in items.items() -%}
        {{ scope }}
        {%- if item.index is not none %} = 0x{{ '%08x' % item.index }}{% endif %}
        {%- if not loop.last -%}
        ,{% endif %}
        {%- if item.desc is not none %} // {{ item.desc }}{% endif %}
        {%- if not loop.last %}
        {% endif -%}
        {%- endfor %}
    }
}