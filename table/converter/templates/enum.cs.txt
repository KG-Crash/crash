// DO NOT MODIFY

namespace Shared
{
    {% for enumName, enumSet in dataSet.items() -%}
    public enum {{ enumName }}
    {
        {% for name, desc in enumSet.items() -%}
        {{ name }}
        {%- if not loop.last -%},{%- endif %} // {{ desc }}
        {%- if not loop.last %}

        {% endif %}
        {%- endfor %}
    }
    {%- if not loop.last %}
    {% endif %}
    {%- endfor %}
}
