// DO NOT MODIFY

using System;
using System.Collections.Generic;
using Shared.Type;

namespace Shared.Const
{
    {%- for scope, props in properties.items() %}
    public static class {{ scope }}
    {
        {%- for prop_name, prop_conf in props.items() %}
        public static readonly {{ prop_conf.type }} {{ prop_name }} = {{ prop_conf.value }};
        {%- endfor %}
    }
    {%- if not loop.last %}
    {% endif -%}
    {%- endfor %}
}
