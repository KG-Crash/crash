// DO NOT MODIFY

package table

import (
	"encoding/json"
	"io/ioutil"
)

func init() {
	if data, err := ioutil.ReadFile("json/{{ name }}.json"); err == nil {
		json.Unmarshal(data, &Table{{ name }})
	}
}

type {{ name }} struct { {% for property in properties %}
    {{ property.name }} {{ property.type }}
    {%- endfor %}
}

var Table{{ name }} {% if primary is not none -%}
map[{{ primary }}]{{ name }}
{%- elif index -%}
map[{{ index }}][]{{ name }}
{%- else -%}
[] {{ name }}
{%- endif -%}